{
  "name": "cloud",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp dev",
    "build": "vp build",
    "types": "wrangler types",
    "deploy": "pnpm run build && wrangler d1 migrations apply cloudcalf --remote --config wrangler.prod.toml && wrangler deploy --config wrangler.prod.toml",
    "migrate": "wrangler d1 migrations apply cloudcalf --local"
  },
  "dependencies": {
    "@heroui/react": "catalog:",
    "@heroui/styles": "catalog:",
    "@tailwindcss/vite": "catalog:",
    "cloudcalf": "workspace:*",
    "db": "workspace:*",
    "hono": "catalog:",
    "lucide-react": "catalog:",
    "oauth": "workspace:*",
    "observability": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:",
    "tailwindcss": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "vite": "catalog:"
  }
}
